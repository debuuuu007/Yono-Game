// import { useNavigate } from 'react-router-dom';

const SeeAllGamesButton = () => {
  // const navigate = useNavigate();

  // const handleClick = () => {
  // //   navigate('/games');
  // };

  return (
    <button
      // onClick={handleClick}
      className="px-4 py-2 bg-orange-600 text-white rounded hover:bg-blue-600 transition-colors"
    >
      SEE ALL GAMES
    </button>
  );
};

export default SeeAllGamesButton;
